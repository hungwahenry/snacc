/**
 * Main database types
 * Re-exports the unified Database interface from domain structure
 */

export type { Database, Json, AuthDatabase, ProfileDatabase, SocialDatabase } from './database/index'